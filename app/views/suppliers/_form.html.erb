<%= form_with(model: supplier, class: "form-horizontal") do |form| %>
  <% if supplier.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(supplier.errors.count, "error") %> prohibited this supplier from being saved:</h2>

      <ul>
        <% supplier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-6">

        <div class="form-group row">
          <%= form.label :name, "仕入れ先名", class: "col-sm-4 col-form-label text-sm-right" %>
          <div class="col-sm-8">
            <%= form.text_field :name, class: "form-control" %>
          </div>
        </div>

        <div class="form-group row">
          <%= form.label :email, "メールアドレス", class: "col-sm-4 col-form-label text-sm-right" %>
          <div class="col-sm-8">
            <%= form.text_field :email, class: "form-control" %>
          </div>
        </div>

        <div class="form-group row">
          <div class="col-sm-8 offset-sm-4">
            <%= form.submit "登録", class: 'btn btn-primary' %>
          </div>
        </div>

      </div>
    </div>
  </div>

<% end %>
