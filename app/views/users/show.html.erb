<p id="notice"><%= notice %></p>

<h2>詳細</h2>
<table>
  <thead>
    <tr>
      <th>管理者</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @user.name %></td>
        <td><%= link_to '編集', edit_user_path(@user) %></td>
      </tr>
  </tbody>
</table>
<%= link_to '戻る', users_path, class: 'btn btn-primary register-btn' %>
